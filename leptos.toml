# Configuration simple pour cargo-leptos
[package]
name = "solana-dashboard"
version = "0.1.0"

[site]
site-root = "target/site"
site-pkg-dir = "pkg"

[server]
site-addr = "127.0.0.1:3000"
reload-port = 3001

[build]
bin-features = ["ssr"]
lib-features = ["csr"]
watch = false
env = "DEV"